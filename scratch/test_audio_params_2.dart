import 'package:quran/quran.dart' as q;

void main() {
  // Try calling with a reciter. If it fails analysis, then it doesn't support it.
  // String url = q.getAudioURLByVerse(1, 1, reciter: 'ar.shaatree'); 
}
